# إعداد قاعدة البيانات في Supabase

## تعليمات الإعداد

### 1. إنشاء مشروع Supabase جديد
1. اذهب إلى [Supabase.com](https://supabase.com)
2. أنشئ حساب جديد أو سجل دخول
3. اضغط على "New Project"
4. اختر اسماً للمشروع وكلمة مرور قوية

### 2. تشغيل إعداد قاعدة البيانات

1. في لوحة تحكم Supabase، اذهب إلى **SQL Editor**
2. انسخ محتوى ملف `supabase-schema.sql`
3. الصق الكود في SQL Editor
4. اضغط **RUN**
5. تأكد من ظهور رسالة النجاح

### 3. الحصول على مفاتيح API

1. اذهب إلى **Settings** > **API**
2. انسخ:
   - **Project URL**
   - **anon public key**

### 4. إعداد متغيرات البيئة

انسخ ملف `.env.example` إلى `.env` وملء القيم:

```env
REACT_APP_SUPABASE_URL=https://your-project-id.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your_supabase_anon_key
```

## ما يقوم به الإعداد

### الجداول المُنشأة:

1. **users** - بيانات المستخدمين الأساسية
2. **family_members** - أفراد العائلة

### الأمان (RLS):

- Row Level Security مُفعل
- كل مستخدم يرى بياناته فقط
- صلاحيات محددة بدقة

### الفهارس:

- فهارس للبحث السريع
- فهرس البحث النصي باللغة العربية

### المحفزات (Triggers):

- تحديث تلقائي لحقل `updated_at`

## استكشاف الأخطاء

### إذا فشل الإعداد:
1. تأكد من أن المشروع جديد
2. حاول تشغيل الكود على دفعات
3. تحقق من الأخطاء في SQL Editor

### للتحقق من نجاح الإعداد:
```sql
-- فحص الجداول
SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'public' AND table_name IN ('users', 'family_members');

-- فحص الصلاحيات
SELECT * FROM information_schema.role_table_grants 
WHERE table_name IN ('users', 'family_members');
```

## ملاحظات مهمة

- 🔒 **RLS مُفعل**: كل مستخدم يرى بياناته فقط
- 🚀 **أداء محسن**: فهارس للبحث السريع
- 🔄 **تحديث تلقائي**: timestamps محدثة تلقائياً
- 🔍 **البحث العربي**: دعم البحث النصي بالعربية
